# axiomops / design

### Highest-Value Incremental Improvement
The highest-value incremental improvement that can be shipped in under 2 hours for the AxiomOps project is to implement the HF CDN Bypass pattern to avoid rate-limit blocks during dataset training.

### Implementation Plan
1. **Identify Dataset Repositories**: List the dataset repositories used in the AxiomOps project that are hosted on Hugging Face.
2. **Modify Training Scripts**: Update the training scripts to use the HF CDN Bypass pattern by downloading dataset files directly from the CDN instead of using the Hugging Face API.
3. **Embed File List in Training Script**: Pre-list the file paths for each dataset repository and embed the list in the training script to avoid API calls during data loading.
4. **Test and Verify**: Test the modified training scripts to ensure that they can download dataset files successfully from the HF CDN and train models without encountering rate-limit blocks.

### Code Snippets
```python
import requests
import json

# Define the dataset repository and file path
dataset_repo = "username/dataset-name"
file_path = "path/to/dataset/file.parquet"

# Download dataset file from HF CDN
cdn_url = f"https://huggingface.co/datasets/{dataset_repo}/resolve/main/{file_path}"
response = requests.get(cdn_url)

# Save dataset file to local storage
with open(file_path, "wb") as f:
    f.write(response.content)
```

```python
import json

# Pre-list file paths for dataset repository
file_paths = []
dataset_repo = "username/dataset-name"
date_folder = "2023-03-01"

# Get list of files in dataset repository
response = requests.get(f"https://huggingface.co/datasets/{dataset_repo}/resolve/main/{date_folder}")
files = response.json()

# Extract file paths and save to list
for file in files:
    file_path = file["path"]
    file_paths.append(file_path)

# Save file list to JSON file
with open("file_list.json", "w") as f:
    json.dump(file_paths, f)
```

```python
import json

# Load file list from JSON file
with open("file_list.json", "r") as f:
    file_paths = json.load(f)

# Use file list in training script
for file_path in file_paths:
    # Download dataset file from HF CDN
    cdn_url = f"https://huggingface.co/datasets/{dataset_repo}/resolve/main/{file_path}"
    response = requests.get(cdn_url)

    # Save dataset file to local storage
    with open(file_path, "wb") as f:
        f.write(response.content)

    # Train model using dataset file
    # ...
```

### Example Use Case
To use the HF CDN Bypass pattern in the AxiomOps project, follow these steps:

1. Identify the dataset repositories used in the project and modify the training scripts to use the HF CDN Bypass pattern.
2. Pre-list the file paths for each dataset repository and embed the list in the training script.
3. Test and verify that the modified training scripts can download dataset files successfully from the HF CDN and train models without encountering rate-limit blocks.

By implementing the HF CDN Bypass pattern, the AxiomOps project can avoid rate-limit blocks during dataset training and improve the overall efficiency of the training process.
