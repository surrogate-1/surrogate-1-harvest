# airship / quality

### Highest-Value Incremental Improvement for Airship Quality
#### Problem Statement
The airship project requires improvements in its quality, focusing on patterns and lessons learned from past experiences. The goal is to identify and implement the highest-value incremental improvement that can be shipped in under 2 hours.

#### Proposed Solution
Based on the provided patterns and lessons learned, the highest-value incremental improvement for airship quality is to **implement a reliable dataset ingestion pipeline**. This involves:

1. **Bypassing HF API rate limits**: Utilize the HF CDN to download dataset files without authorization headers, reducing the likelihood of rate limit blocks.
2. **Pre-listing file paths**: Perform a single API call to list file paths for a date folder, saving the list to JSON, and embedding it in the training script.
3. **Studio reuse**: Reuse existing Lightning Studios instead of recreating them, saving 80hr/mo quota.

#### Implementation Plan
To implement this solution, follow these steps:

1. **Update the dataset ingestion script**:
	* Use the HF CDN to download dataset files.
	* Pre-list file paths for a date folder using a single API call.
	* Save the list to JSON and embed it in the training script.
2. **Modify the Lightning Studio creation process**:
	* Check for existing Running studios before creating a new one.
	* Reuse existing studios instead of recreating them.
3. **Test and verify the changes**:
	* Run the updated dataset ingestion script and verify that it bypasses HF API rate limits.
	* Test the studio reuse functionality and ensure that it saves quota.

#### Code Snippets
```bash
# Update dataset ingestion script to use HF CDN
hf_cdn_url="https://huggingface.co/datasets/{repo}/resolve/main/{path}"
dataset_files=$(curl -s "$hf_cdn_url" | jq -r '.[] | .filename')

# Pre-list file paths for a date folder
file_paths=$(curl -s "https://huggingface.co/api/v1/datasets/{repo}/tree?path={date_folder}&recursive=false" | jq -r '.[] | .path')

# Save file paths to JSON and embed in training script
echo "$file_paths" > file_paths.json
```

```python
# Modify Lightning Studio creation process to reuse existing studios
import lightning

def create_studio(name):
    for s in lightning.Teamspace.studios:
        if s.name == name and s.status == 'Running':
            return s
    # Create a new studio if none exist
    return lightning.Studio(create_ok=True)
```
#### Expected Outcome
The proposed solution is expected to improve the airship quality by:

* Reducing the likelihood of HF API rate limit blocks
* Saving 80hr/mo quota by reusing existing Lightning Studios
* Improving the overall reliability and efficiency of the dataset ingestion pipeline

This solution can be implemented and tested within the 2-hour time frame, providing a high-value incremental improvement for airship quality.
