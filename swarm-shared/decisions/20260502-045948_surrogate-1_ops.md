# surrogate-1 / ops

### Diagnosis
* The project lacks a robust implementation for data ingestion and training, relying heavily on the Hugging Face API with rate limits.
* There is inadequate reuse of existing Lightning Studio instances, leading to wasted quota and potential training interruptions.
* The project does not utilize the HF CDN bypass effectively, resulting in unnecessary API calls and rate limit issues.
* The training pipeline is not optimized for performance, leading to potential bottlenecks and inefficiencies.
* The project does not have a comprehensive solution for handling errors and exceptions, which can lead to training failures and downtime.

### Proposed change
The proposed change is to implement the HF CDN bypass and optimize the training pipeline to reduce API calls and reuse existing Lightning Studio instances. This will involve modifying the `train.py` script to use the HF CDN bypass and implementing a studio reuse mechanism.

### Implementation
To implement the proposed change, the following steps can be taken:
1. Modify the `train.py` script to use the HF CDN bypass by downloading dataset files from `https://huggingface.co/datasets/{repo}/resolve/main/{path}` instead of using the Hugging Face API.
2. Implement a studio reuse mechanism by listing existing Lightning Studio instances and reusing running ones before creating new ones.
3. Optimize the training pipeline to reduce API calls and improve performance.

Code snippet:
```python
import os
import json
import requests

# Define the dataset repository and path
repo = "username/dataset"
path = "path/to/dataset"

# Download dataset files from HF CDN bypass
def download_dataset_files(repo, path):
    url = f"https://huggingface.co/datasets/{repo}/resolve/main/{path}"
    response = requests.get(url)
    if response.status_code == 200:
        return response.content
    else:
        raise Exception(f"Failed to download dataset files: {response.status_code}")

# Implement studio reuse mechanism
def reuse_studio(studio_name):
    studios = Teamspace.studios
    for studio in studios:
        if studio.name == studio_name and studio.status == "Running":
            return studio
    return None

# Optimize training pipeline
def train_model():
    # Download dataset files from HF CDN bypass
    dataset_files = download_dataset_files(repo, path)
    
    # Reuse existing Lightning Studio instance
    studio = reuse_studio("studio_name")
    if studio is None:
        # Create a new Lightning Studio instance
        studio = Teamspace.create_studio("studio_name")
    
    # Train the model
    # ...

# Train the model
train_model()
```
### Verification
To verify that the proposed change works, the following steps can be taken:
1. Run the `train.py` script and verify that the dataset files are downloaded from the HF CDN bypass.
2. Verify that existing Lightning Studio instances are reused correctly.
3. Monitor the training pipeline and verify that API calls are reduced and performance is improved.
4. Test the training pipeline with different datasets and models to ensure that it works correctly and efficiently.
