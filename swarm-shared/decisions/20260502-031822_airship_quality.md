# airship / quality

### Highest-Value Incremental Improvement for Airship Quality
#### Implementation Plan

Based on the provided patterns and lessons learned, the highest-value incremental improvement for Airship quality is to implement a robust data ingestion pipeline that leverages the Hugging Face (HF) CDN to bypass API rate limits. This improvement will enable the Surrogate AI service to efficiently process large datasets without being blocked by API rate limits.

#### Code Snippets

To implement this improvement, we will modify the `train.py` script to use the HF CDN for data ingestion. We will also create a new script, `ingest_data.py`, to handle the data ingestion process.

```python
# ingest_data.py
import json
import requests

def ingest_data(repo, path):
    # Get the list of files in the repository
    files = requests.get(f"https://huggingface.co/datasets/{repo}/resolve/main/{path}").json()

    # Download each file individually
    for file in files:
        file_path = f"https://huggingface.co/datasets/{repo}/resolve/main/{path}/{file['name']}"
        response = requests.get(file_path)
        with open(file['name'], 'wb') as f:
            f.write(response.content)

# train.py
import pandas as pd
from ingest_data import ingest_data

def train_model():
    # Ingest data using the HF CDN
    ingest_data("axentx/airship", "data")

    # Load the ingested data
    data = pd.read_parquet("data.parquet")

    # Train the model
    # ...
```

#### Steps to Implement

1. Create a new script, `ingest_data.py`, to handle the data ingestion process using the HF CDN.
2. Modify the `train.py` script to use the `ingest_data.py` script for data ingestion.
3. Update the `docker-compose.yml` file to include the new `ingest_data.py` script in the Surrogate AI service.
4. Test the new data ingestion pipeline to ensure it is working correctly.

#### Expected Outcome

The expected outcome of this improvement is a significant reduction in API rate limit errors and a increase in the efficiency of the data ingestion process. This will enable the Surrogate AI service to process large datasets more efficiently and improve the overall quality of the Airship platform.

#### Time Estimate

The estimated time to implement this improvement is less than 2 hours. The majority of the time will be spent modifying the `train.py` script and creating the new `ingest_data.py` script.
