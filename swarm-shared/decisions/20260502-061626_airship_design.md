# airship / design

**Highest-Value Incremental Improvement: Bypass HF API Rate Limit for Dataset Training using HF CDN**

**Implementation Plan:**

1. **Update `train.py` to use HF CDN for dataset files**
   - Use `hf_hub_download` to download files from CDN instead of API
   - Save list of file paths to JSON and embed in `train.py`
   - Use `list_repo_tree` with `recursive=False` to get file list
2. **Update `dataset-mirror` to project to {prompt, response} only before upload**
   - Use `hf_hub_download` to download files from CDN
   - Project to {prompt, response} only before upload
   - Move attribution to filename pattern (`batches/mirror-merged/{date}/{slug}.parquet`)
3. **Update `knowledge-rag` to use CDN downloads for hub and related docs**
   - Use `hf_hub_download` to download files from CDN
   - Query top hub and related docs using CDN downloads

**Code Snippets:**

```python
# train.py
import os
import json
import hf_hub_download

# Get list of file paths from JSON
with open('file_list.json', 'r') as f:
    file_paths = json.load(f)

# Download files from CDN
for file_path in file_paths:
    hf_hub_download(file_path)

# Project to {prompt, response} only before upload
# ...

# dataset-mirror
import hf_hub_download

# Download files from CDN
file_paths = hf_hub_download.list_repo_tree(path, recursive=False)

# Project to {prompt, response} only before upload
# ...

# Move attribution to filename pattern
import os
import datetime

# Get current date
current_date = datetime.datetime.now().strftime('%Y%m%d')

# Get slug from file path
slug = os.path.basename(file_path)

# Move attribution to filename pattern
new_filename = f'batches/mirror-merged/{current_date}/{slug}.parquet'
os.rename(file_path, new_filename)

# knowledge-rag
import os
import requests

# Use CDN downloads for hub and related docs
hub_url = f'https://huggingface.co/datasets/{repo}/resolve/main/{hub_path}'
hub_path = hf_hub_download(hub_url)
# Query top hub and related docs using CDN downloads...
```

**Expected Outcome:**

* Bypass HF API rate limit for dataset training using HF CDN
* Improve training speed and reduce API rate limit errors
* Simplify dataset ingestion and reduce complexity

**Estimated Time:** 2 hours

**Tags:** #huggingface #cdn #rate-limit-bypass #training

**Additional Recommendations:**

* Consider implementing a caching mechanism to store downloaded files from the CDN, reducing the need for repeated downloads.
* Use a more robust and efficient way to handle file downloads and projections, such as using a parallel processing library like `joblib` or `dask`.
* Monitor and analyze the performance of the updated training pipeline to identify areas for further optimization and improvement.

**Conclusion:**

By implementing the proposed changes, we can bypass the HF API rate limit for dataset training using the HF CDN, improving training speed and reducing API rate limit errors. The updated training pipeline will also simplify dataset ingestion and reduce complexity. With careful planning and execution, we can achieve significant improvements in training efficiency and reliability.
