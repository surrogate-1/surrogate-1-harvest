# vanguard / ops

### 1. **Diagnosis**
* The Vanguard project lacks a comprehensive solution to handle HF API rate limits, which can block dataset training and hinder the project's progress.
* The current implementation does not utilize the HF CDN bypass strategy, which can download public dataset files without being limited by the HF API rate limit.
* The project's training scripts do not pre-list file paths once and embed them in the training script, resulting in repeated API calls and potential rate limit issues.
* The project does not reuse existing Lightning Studios, resulting in wasted quota and potential delays in training.
* The project's training scripts do not handle Lightning idle stop kills training, which can cause training processes to die unexpectedly.

### 2. **Proposed change**
The proposed change is to implement the HF CDN bypass strategy and pre-list file paths once, then embed them in the training script. This change will be made in the `train.py` file located in the `/opt/axentx/vanguard` directory.

### 3. **Implementation**
To implement this change, follow these steps:
1. **Pre-list file paths**: Make a single API call to `list_repo_tree(path, recursive=False)` for one date folder and save the list to a JSON file.
2. **Embed file list in training script**: Modify the `train.py` file to read the pre-listed file paths from the JSON file and use them to download the dataset files from the HF CDN.
3. **Use HF CDN bypass**: Modify the `train.py` file to download public dataset files from the HF CDN using the `https://huggingface.co/datasets/{repo}/resolve/main/{path}` URL, without making API calls.

Example code snippet:
```python
import json
import requests

# Pre-list file paths
def pre_list_file_paths(repo, path):
    response = requests.get(f"https://huggingface.co/api/v1/datasets/{repo}/tree?path={path}&recursive=false")
    file_paths = response.json()["files"]
    with open("file_paths.json", "w") as f:
        json.dump(file_paths, f)

# Embed file list in training script
def load_dataset(file_paths):
    dataset = []
    for file_path in file_paths:
        response = requests.get(f"https://huggingface.co/datasets/{repo}/resolve/main/{file_path}")
        dataset.append(response.json())
    return dataset

# Use HF CDN bypass
def download_dataset(file_paths):
    dataset = []
    for file_path in file_paths:
        response = requests.get(f"https://huggingface.co/datasets/{repo}/resolve/main/{file_path}")
        dataset.append(response.json())
    return dataset

# Load pre-listed file paths
with open("file_paths.json", "r") as f:
    file_paths = json.load(f)

# Download dataset using HF CDN bypass
dataset = download_dataset(file_paths)
```
### 4. **Verification**
To verify that the change works, follow these steps:
1. Run the modified `train.py` file and check that the dataset is downloaded successfully from the HF CDN.
2. Monitor the API calls made by the training script and verify that the number of API calls is reduced.
3. Check the Lightning Studio quota and verify that it is not wasted due to repeated studio creations.
4. Run the training script multiple times and verify that the training process is not killed by Lightning idle stop.
