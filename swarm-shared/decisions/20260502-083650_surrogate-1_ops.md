# surrogate-1 / ops

### Diagnosis
* The project lacks a robust implementation for handling Hugging Face API rate limits, which can block dataset training.
* The existing implementation may not be reusing existing Lightning Studio instances efficiently, leading to wasted resources.
* The project does not have a mechanism to bypass the Hugging Face API rate limit for dataset training.
* The project may be running heavy computations on the Mac, which can lead to performance issues and inefficiencies.
* The project does not have a mechanism to handle Lightning idle stop kills training.

### Proposed change
The proposed change is to implement a mechanism to bypass the Hugging Face API rate limit for dataset training by using the HF CDN bypass. This can be achieved by modifying the `train.py` script to download dataset files from the HF CDN instead of using the Hugging Face API.

### Implementation
To implement this change, we need to modify the `train.py` script as follows:
```python
import json
import requests

# Download dataset files from HF CDN
def download_dataset_files(repo, path):
    url = f"https://huggingface.co/datasets/{repo}/resolve/main/{path}"
    response = requests.get(url)
    return response.content

# Load dataset files
def load_dataset(repo, path):
    file_list = json.load(open("file_list.json"))
    dataset = []
    for file in file_list:
        file_path = file["path"]
        file_content = download_dataset_files(repo, file_path)
        dataset.append(file_content)
    return dataset

# Train model
def train_model(dataset):
    # Train model using the dataset
    pass

# Main function
def main():
    repo = "axentx/surrogate-1"
    path = "data/train"
    file_list = json.load(open("file_list.json"))
    dataset = load_dataset(repo, path)
    train_model(dataset)

if __name__ == "__main__":
    main()
```
We also need to create a `file_list.json` file that contains the list of dataset files to download. This file can be created by running a script that uses the Hugging Face API to list the files in the dataset repository.
```python
import json
import requests

# List dataset files
def list_dataset_files(repo, path):
    url = f"https://huggingface.co/api/v1/datasets/{repo}/tree?path={path}&recursive=False"
    response = requests.get(url)
    return response.json()

# Create file list
def create_file_list(repo, path):
    file_list = list_dataset_files(repo, path)
    with open("file_list.json", "w") as f:
        json.dump(file_list, f)

# Main function
def main():
    repo = "axentx/surrogate-1"
    path = "data/train"
    create_file_list(repo, path)

if __name__ == "__main__":
    main()
```
### Verification
To verify that the change works, we can run the `train.py` script and check that the dataset files are downloaded from the HF CDN instead of using the Hugging Face API. We can also check that the model is trained successfully using the downloaded dataset files.

We can verify the change by checking the following:

* The `file_list.json` file is created correctly and contains the list of dataset files to download.
* The `train.py` script downloads the dataset files from the HF CDN instead of using the Hugging Face API.
* The model is trained successfully using the downloaded dataset files.
* The Hugging Face API rate limit is not exceeded during the training process.
