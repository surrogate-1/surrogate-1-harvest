# Costinel / ops

**Highest-value incremental improvement that can ship in <2h:**

**Fix: Ensure wrapper script has proper Bash shebang, is executable, invoke via Bash, set SHELL=/bin/bash in crontab**

**Implementation Plan:**

1. **Verify wrapper script**: Check if the wrapper script for opus pr reviewer has a proper Bash shebang (`#!/usr/bin/env bash`) at the top.
2. **Make script executable**: Run `chmod +x <script_name>` to make the script executable.
3. **Invoke script via Bash**: Update the crontab to invoke the script via Bash: `bash <script_name> "$@"`
4. **Set SHELL=/bin/bash in crontab**: Update the crontab to set `SHELL=/bin/bash` to ensure the script is executed with Bash.

**Code Snippet:**

Update the wrapper script to have a proper Bash shebang:
```bash
#!/usr/bin/env bash
```
Update the crontab to invoke the script via Bash:
```bash
0 * * * * bash /path/to/script.sh "$@"
```
Update the crontab to set `SHELL=/bin/bash`:
```bash
SHELL=/bin/bash
```
**Commit Message:**

`fix: ensure wrapper script has proper Bash shebang, is executable, invoke via Bash, set SHELL=/bin/bash in crontab`
