# surrogate-1 / ops

### Diagnosis
* The project lacks a robust implementation for data ingestion and training, relying heavily on the Hugging Face API with rate limits.
* There is inadequate reuse of existing Lightning Studio instances, leading to wasted quota and potential training interruptions.
* The current setup does not utilize the HF CDN bypass effectively, resulting in unnecessary API calls and potential rate limit issues.
* The project's training pipeline is not optimized for performance, with potential bottlenecks in data loading and processing.
* The lack of a comprehensive monitoring and logging system makes it difficult to identify and address issues in real-time.

### Proposed change
The proposed change is to implement the HF CDN bypass and optimize the training pipeline to reduce API calls and improve performance. This will involve modifying the `train.py` script to use the HF CDN bypass for data loading and implementing a caching mechanism to reduce the number of API calls.

### Implementation
To implement the HF CDN bypass, we will modify the `train.py` script as follows:
```python
import os
import json
from datasets import load_dataset

# Load dataset from HF CDN
def load_dataset_from_cdn(repo_id, dataset_name):
    url = f"https://huggingface.co/datasets/{repo_id}/resolve/main/{dataset_name}"
    dataset = load_dataset(url, streaming=True)
    return dataset

# Load dataset from HF API (fallback)
def load_dataset_from_api(repo_id, dataset_name):
    dataset = load_dataset(f"{repo_id}/{dataset_name}", streaming=True)
    return dataset

# Load dataset using HF CDN bypass
def load_dataset(repo_id, dataset_name):
    try:
        dataset = load_dataset_from_cdn(repo_id, dataset_name)
    except Exception as e:
        print(f"Error loading dataset from CDN: {e}")
        dataset = load_dataset_from_api(repo_id, dataset_name)
    return dataset

# Example usage
repo_id = "axentx/surrogate-1"
dataset_name = "train"
dataset = load_dataset(repo_id, dataset_name)
```
To optimize the training pipeline, we will implement a caching mechanism using a JSON file to store the list of files in the dataset. This will reduce the number of API calls and improve performance.
```python
import json

# Load file list from cache
def load_file_list_from_cache(cache_file):
    with open(cache_file, "r") as f:
        file_list = json.load(f)
    return file_list

# Save file list to cache
def save_file_list_to_cache(cache_file, file_list):
    with open(cache_file, "w") as f:
        json.dump(file_list, f)

# Example usage
cache_file = "file_list.json"
file_list = load_file_list_from_cache(cache_file)
if not file_list:
    file_list = load_dataset_from_cdn(repo_id, dataset_name)
    save_file_list_to_cache(cache_file, file_list)
```
### Verification
To verify that the implementation works, we can run the modified `train.py` script and monitor the API calls and performance. We can also check the cache file to ensure that the file list is being stored and loaded correctly.

* Run the modified `train.py` script and monitor the API calls using the Hugging Face API dashboard.
* Check the cache file to ensure that the file list is being stored and loaded correctly.
* Monitor the performance of the training pipeline using metrics such as training time and memory usage.
* Verify that the HF CDN bypass is working correctly by checking the dataset loading time and API call count.
