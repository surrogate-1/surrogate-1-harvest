# axiomops / ops

**Synthesized Solution: Implementing HF CDN Bypass Pattern for Efficient Dataset Training**

After analyzing the proposals, the best approach to achieve the highest-value incremental improvement in <2h is to implement the HF CDN Bypass pattern. This solution enables the dataset training process to avoid rate-limit blocks, resulting in faster and more efficient training.

**Implementation Plan:**

1. **Identify the dataset repository**: Determine the repository containing the dataset to be used for training.
2. **Get the list of file paths**: Use the `list_repo_tree` API call to retrieve the list of file paths for the dataset repository. Make this call from a location that won't block the training process (e.g., after the rate-limit window clears).
3. **Save the list to JSON**: Save the list of file paths to a JSON file that can be embedded in the training script.
4. **Modify the training script**: Update the training script to use CDN-only fetches with zero API calls during data load. Utilize the `https://huggingface.co/datasets/{repo}/resolve/main/{path}` URL pattern to download dataset files directly from the CDN.

**Code Snippets:**

To get the list of file paths:
```bash
repo="axentx/dataset"
path="data"
file_list=$(curl -X GET \
  https://huggingface.co/api/v1/datasets/${repo}/tree/${path} \
  -H 'Authorization: Bearer YOUR_API_TOKEN' \
  -H 'Content-Type: application/json')
echo "${file_list}" > file_list.json
```

To modify the training script:
```python
import json
import requests

# Load the list of file paths from JSON
with open('file_list.json') as f:
  file_list = json.load(f)

# Download dataset files directly from CDN
for file in file_list:
  file_path = f"https://huggingface.co/datasets/axentx/dataset/resolve/main/{file['path']}"
  response = requests.get(file_path)
  # Process the downloaded file
  # ...
```

**Expected Outcome:**
By implementing the HF CDN Bypass pattern, the dataset training process will no longer be blocked by API rate limits, allowing for faster and more efficient training. This improvement can be shipped in <2h, making it a high-value incremental improvement for the project.

**Benefits:**

* Avoids rate-limit blocks during dataset training
* Reduces overall training time
* Improves the efficiency of the system
* Can be shipped in <2h, making it a high-value incremental improvement

By following this synthesized solution, you can efficiently implement the HF CDN Bypass pattern and improve the performance of your dataset training process.
